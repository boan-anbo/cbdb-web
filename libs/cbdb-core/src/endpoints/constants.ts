/**
 * Global API constants shared across all endpoints
 */

/**
 * Global API prefix for all endpoints
 * Set to 'api' because NestJS backend uses this as global prefix
 * The baseUrl should NOT include /api to avoid double prefix
 */
export const API_PREFIX = 'api';