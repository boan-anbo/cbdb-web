# Application database path (for app state, settings, etc.)
APP_DB_PATH=./cbdb-desktop.db

# User's CBDB database path (will be set dynamically when user opens a file)
CBDB_PATH=

NODE_ENV=development
# PORT=18019  # Optional - defaults to 18019 if not specified